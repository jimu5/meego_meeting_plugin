package model

// 控件配置, 这个是存储在 meego 上面的, 我这里应该不需要关注
type ControlSetting struct {
	//gorm.Model
	//ProjectKey      string `json:"project_key" gorm:"size:100"`  // 空间 key
	//WorkItemTypeKey string `json:"work_item_type_key" gorm:"size:100"`  // 工作项
}
